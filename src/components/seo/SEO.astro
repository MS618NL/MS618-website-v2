---
import site from '@/data/site.json';
import { canonicalUrl, ogImageUrl } from '@/utils/seo';
import { getLangFromUrl, getAlternatePath } from '@/i18n';

interface Props {
  title: string;
  seoTitle?: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  ogType?: 'website' | 'article';
  noindex?: boolean;
  rss?: boolean;
  publishDate?: string;
  updatedDate?: string;
  author?: string;
  themeColor?: string;
  /** Whether an English alternate of this page exists. False suppresses the en hreflang. */
  enAlternate?: boolean;
}

const {
  title,
  seoTitle,
  description,
  canonical,
  ogImage,
  ogType = 'website',
  noindex = false,
  rss = false,
  publishDate,
  updatedDate,
  author,
  themeColor = '#0A0A0A',
  enAlternate = true,
} = Astro.props;

const pageTitle = seoTitle ?? `${title} — ${site.name}`;
const canonicalHref = canonical ?? canonicalUrl(Astro.url.pathname);
const ogImageHref = ogImageUrl(ogImage);
const robots = noindex ? 'noindex, nofollow' : 'index, follow';

const lang = getLangFromUrl(Astro.url);
const ogLocale = lang === 'en' ? 'en_US' : 'nl_NL';
const nlHref = site.url + getAlternatePath(Astro.url, 'nl');
const enHref = site.url + getAlternatePath(Astro.url, 'en');
---

<!-- Primary Meta -->
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{pageTitle}</title>
<meta name="description" content={description} />
<meta name="robots" content={robots} />
<meta name="theme-color" content={themeColor} />
<link rel="canonical" href={canonicalHref} />

<!-- Apple PWA -->
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-title" content={site.name} />
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

<!-- Hreflang -->
{!noindex && (
  <>
    <link rel="alternate" hreflang="nl" href={nlHref} />
    {enAlternate && <link rel="alternate" hreflang="en" href={enHref} />}
    <link rel="alternate" hreflang="x-default" href={nlHref} />
  </>
)}

<!-- Open Graph -->
<meta property="og:type" content={ogType} />
<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={description} />
<meta property="og:url" content={canonicalHref} />
<meta property="og:image" content={ogImageHref} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:site_name" content={site.name} />
<meta property="og:locale" content={ogLocale} />
{ogType === 'article' && publishDate && (
  <meta property="article:published_time" content={publishDate} />
)}
{ogType === 'article' && updatedDate && (
  <meta property="article:modified_time" content={updatedDate} />
)}
{ogType === 'article' && author && (
  <meta property="article:author" content={author} />
)}

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImageHref} />

<!-- Favicon -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />

<!-- Sitemap discovery -->
<link rel="sitemap" href="/sitemap-index.xml" />

<!-- RSS discovery -->
{rss && (
  <link
    rel="alternate"
    type="application/rss+xml"
    title={`${site.name} — Blog`}
    href={lang === 'en' ? '/en/rss.xml' : '/rss.xml'}
  />
)}
